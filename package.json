{
    "name": "jlisp",
    "version": "0.0.1",
    "devDependencies": {
        "readline-sync": "1.4.7"
    },
    "scripts": {
        "start": "cd dist && node jlisp.js",
        "test": "cd dist/tests && node all_tests.js",
        "full": "npm install && tsc && npm test && npm start"
    }
}
